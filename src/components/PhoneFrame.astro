---
export interface Props {
  label: string;
  class?: string;
}

const { label, class: className = '' } = Astro.props;
---

<div class={`phone-frame ${className}`.trim()} role="img" aria-label={label}>
  <div class="phone-frame__screen">
    <slot />
  </div>
</div>
