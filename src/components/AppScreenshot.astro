---
export interface Props {
  src: string;
  alt: string;
  width?: number;
  height?: number;
}

const { src, alt, width = 828, height = 1684 } = Astro.props;
---

<img src={src} alt={alt} width={width} height={height} class="phone-shot" loading="lazy" />
