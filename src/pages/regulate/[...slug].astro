---
export function getStaticPaths() {
  return [{ params: { slug: undefined } }, { params: { slug: 'geometry' } }];
}

const slug = Astro.params.slug;
const dest = slug
  ? `https://returnapp.org/regulate/${slug}`
  : 'https://returnapp.org/regulate';
return Astro.redirect(dest, 301);
---
